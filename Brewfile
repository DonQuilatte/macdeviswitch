# Homebrew dependencies for MacDeviSwitch development

# Tool for generating CHANGELOG.md from conventional commits
brew "git-cliff"

# Add other essential tools if known (e.g., SwiftLint, clang-format if not managed elsewhere)
# brew "swiftlint"
# brew "clang-format"
